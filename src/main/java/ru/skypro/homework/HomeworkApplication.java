package ru.skypro.homework;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HomeworkApplication {

  public static void main(String[] args) {
    SpringApplication.run(HomeworkApplication.class, args);
  }

  // Dockerfile:

  // FROM adoptopenjdk:11-jre-hotspot
  // Эта строка указывает на базовый образ, который будет использован для создания нового образа Docker. В данном случае
  // это образ OpenJDK 11 с HotSpot JVM от проекта AdoptOpenJDK. Это означает, что ваш контейнер будет использовать
  // Java 11 с этим JVM для запуска приложений.

  // ARG JAR_FILE=target/*.jar
  // Эта строка определяет аргумент для Dockerfile, который используется для указания файла JAR, который будет скопирован
  // в контейнер. В данном случае это файлы JAR из каталога target (обычно_generate место для сборки Maven или Gradle).
  // Звездочка * может соответствовать любому имени файла, что позволяет скопировать любой файл JAR из каталога target.

  // COPY ${JAR_FILE} app.jar
  // Эта команда копирует файл JAR, указанный в аргументе JAR_FILE, из каталога сборки в контейнер под именем app.jar.
  // Это означает, что в контейнере ваше приложение будет доступно под этим именем.
  // Вместе эти строки позволяют создать Docker-образ, который основан на OpenJDK 11 с HotSpot JVM и содержит приложение
  // Java, упакованное в JAR-файл.

  // Инструкция ENTRYPOINT ["java","-jar","/app.jar"] в Dockerfile задаёт основную команду, которая всегда будет
  // выполняться при запуске контейнера. В данном случае это запуск Java-приложения из файла /app.jar с помощью команды
  // java -jar /app.jar.
  // Что означает эта инструкция?
  // ENTRYPOINT — это директива в Dockerfile, которая определяет неизменяемую точку входа в контейнер, то есть команду,
  // которая будет выполняться всегда при запуске контейнера, независимо от параметров, поданных при запуске.
  // В форме ["java","-jar","/app.jar"] используется exec-формат, где каждый элемент массива — это аргумент команды:
  // "java" — исполнитель, "-jar" — опция запуска Java с указанием JAR-файла, "/app.jar" — путь к исполняемому
  // Java-архиву внутри контейнера.
  // Благодаря ENTRYPOINT у пользователя при запуске контейнера не возникает сомнений, что именно будет выполнено —
  // в данном случае запуск конкретного Java-приложения.
  // Для чего это нужно?
  // При таком задании контейнер становится специально настроенным для запуска именно этого Java-приложения.
  // Если кто-то попробует запустить контейнер и передать другой аргумент, то команда java -jar /app.jar всё равно
  // будет выполнена как основная — это фиксирует поведение контейнера.
  // Можно комбинировать с CMD, чтобы добавлять дополнительные параметры в команду запуска при необходимости.
  // Таким образом, строка ENTRYPOINT ["java","-jar","/app.jar"] означает, что при старте контейнера Docker
  // автоматически запустит Java-приложение, находящееся в файле /app.jar. Это позволяет контейнеру служить точкой
  // запуска именно этого приложения.

  // Файл docker-compose.yml
  
  // Ниже приведён пример файла docker-compose.yml, в котором определены два сервиса: образ приложения на языке Java
  // (например, Spring Boot) и образ базы данных MySQL. Этот пример основан на реальных наработках и включает создание
  // собственного образа приложения из Dockerfile и использование официального образа MySQL:
  //
  // version: "3.8"
  //
  // services:
  //   mysqldb:
  //     image: mysql:5.7
  //     restart: unless-stopped
  //     environment:
  //       MYSQL_ROOT_PASSWORD: root password
  //       MYSQL_DATABASE: database
  //       MYSQL_USER: user
  //       MYSQL_PASSWORD: user password
  //     ports:
  //       - "3306:3306"
  //     volumes:
  //       - db_data:/var/lib/mysql
  //
  //   app:
  //     build: ./java-app  # Путь к директории с Dockerfile для Java-приложения
  //     depends_on:
  //       - mysqldb
  //     restart: on-failure
  //     ports:
  //       - "8080:8080"
  //     environment:
  //       SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb:3306/mydatabase?useSSL=false
  //       SPRING_DATASOURCE_USERNAME: user
  //       SPRING_DATASOURCE_PASSWORD: userpassword
  //
  // volumes:
  //   db_data:

  // Объяснение:
  //
  // mysqldb — сервис с MySQL, использует официальный образ mysql:5.7, с настройкой пароля для root, создания базы
  // данных, пользователя и пароля для доступа. Порт 3306 проброшен наружу, данные сохраняются в докер-том db_data.
  // app — сервис приложения на Java, образ которого собирается локально из папки ./java-app, где должен находиться
  // Dockerfile с описанием сборки Java-приложения (например, Spring Boot). Сервис зависит от базы данных
  // (через depends_on), порты проброшены (например, 8080), переменные окружения передают параметры подключения к базе.
  // Том db_data обеспечивает персистентность данных MySQL.
  // Такой файл можно использовать для запуска приложения и базы через команду:
  // docker-compose up
  // Это типовой паттерн для докеризации Java-приложения с подключением к MySQL через docker-compose, он основан на
  // примерах из статьи и репозиториев, например Bezkoder туториал и открытых примерах в GitHub[1][3][4].

  // Инструкция ENTRYPOINT ["java","-jar","/app.jar"] в Dockerfile задаёт основную команду, которая всегда будет
  // выполняться при запуске контейнера. В данном случае это запуск Java-приложения из файла /app.jar с помощью команды
  // java -jar /app.jar.
  // Что означает эта инструкция?
  // ENTRYPOINT — это директива в Dockerfile, которая определяет неизменяемую точку входа в контейнер, то есть команду,
  // которая будет выполняться всегда при запуске контейнера, независимо от параметров, поданных при запуске.
  // В форме ["java","-jar","/app.jar"] используется exec-формат, где каждый элемент массива — это аргумент команды:
  // "java" — исполнитель, "-jar" — опция запуска Java с указанием JAR-файла, "/app.jar" — путь к исполняемому
  // Java-архиву внутри контейнера.
  // Благодаря ENTRYPOINT у пользователя при запуске контейнера не возникает сомнений, что именно будет выполнено —
  // в данном случае запуск конкретного Java-приложения.
  // Для чего это нужно?
  // При таком задании контейнер становится специально настроенным для запуска именно этого Java-приложения.
  // Если кто-то попробует запустить контейнер и передать другой аргумент, то команда java -jar /app.jar всё равно
  // будет выполнена как основная — это фиксирует поведение контейнера.
  // Можно комбинировать с CMD, чтобы добавлять дополнительные параметры в команду запуска при необходимости.
  // Таким образом, строка ENTRYPOINT ["java","-jar","/app.jar"] означает, что при старте контейнера Docker
  // автоматически запустит Java-приложение, находящееся в файле /app.jar. Это позволяет контейнеру служить точкой
  // запуска именно этого приложения.

}
