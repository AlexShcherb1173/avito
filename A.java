# Ads Platform

## Описание проекта
Этот проект представляет собой платформу для управления объявлениями. Пользователи могут создавать, редактировать, удалять объявления и комментарии. Также предусмотрены роли для пользователей и администраторов.

## Участники проекта
- Евгений (Роль: разработчик)
- [Другие участники]

## Используемые технологии и библиотеки
- **Spring Boot** — для создания приложения
- **Spring Security** — для аутентификации и авторизации
- **PostgreSQL** — для хранения данных
- **Liquibase** — для миграции БД
- **MapStruct** — для маппинга сущностей в DTO и обратно
- **Swagger** — для документации API
- **JUnit** и **Spring Boot Test** — для тестирования

## Запуск проекта

1. Клонируйте репозиторий:
    ```bash
    git clone <repo_url>
    ```
2. Настройте подключение к базе данных PostgreSQL в файле `application.properties`.
3. Запустите приложение:
    ```bash
    mvn spring-boot:run
    ```
4. Swagger UI доступен по адресу:
    ```
    http://localhost:8080/swagger-ui.html
    ```

